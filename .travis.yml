# 0a2b636398cb84cf0a27c648f88636defe12ab76
# 配置语言及相应版本
language: node_js
node_js: stable

# 项目所在分支
branches:
  only:
  - hexo

# 安装依赖
install:
  - npm install

#before_script:
# - npm install -g gulp

script:
  - hexo g

after_script:
  - cd ./public
  - git init
  - git config user.name "jiaren.tang"
  - git config user.email "shmilybaozi@163.com"
  - git add .
  - git commit -m "Update docs"
  - git push --force --quiet "https://${gitToken}@${GH_REF}" master:master
# E: Build LifeCycle

env:
  global:
    - GH_REF: github.com/shmilybaozi/shmilybaozi.github.io.git
